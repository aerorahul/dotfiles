###############################################################
# Set user-defined aliases

# Aliases to make sure that the copy, move, and remove programs
# ask you for confirmation. If you don't like this (i.e., if
# you know what you're doing), delete these lines.
alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -i'

# Commonly used aliases
alias h='history'
alias df='df -h'
alias du='du -hs'
alias grep='grep --color'

# Aliases used with man
alias appropos='man -k'
alias fman='nroff -man \!* | more -s'
alias fme='tbl \!* | nroff -me |more -s'

#	Aliases for ls
alias ls='/bin/ls -G'
alias l='ls -l'
alias ll='ls -l'
alias lh='ls -lh'
alias la='ls -la'
alias ld='ls -lt  | grep drw'
alias lda='ls -lat | grep drw'
alias lrt='ls -lrt'
alias lrta='ls -lart'
alias l1='ls -1'
alias l1rt='ls -1rt'

# Aliases for changing directories
alias echopwd='echo "changing directory to ... `pwd`"'

# Aliases for applications
alias ncview='ncview -nc 32 -extra'

# Aliases for ssh
alias sshx='/usr/bin/ssh -X'
alias sshy='/usr/bin/ssh -Y'
alias sshxy='/usr/bin/ssh -X -Y'

# Aliases for westeros's machines; See ~/.ssh/config for settings
alias dornelocal='ssh dorne.local'
alias dorne='ssh dorne'

# Aliases for NCCS machines (create a tunnel with tnccs first)
alias nccs='sshxy -l rmahajan login.nccs.nasa.gov'
alias discover='sshxy -l rmahajan discover'
alias dali='sshxy -l rmahajan dali'
alias dali-gpu='sshxy -l rmahajan dali-gpu'
alias dirac='sshxy -l rmahajan dirac'

# Aliases for SSEC machines
alias s4='sshxy s4'
alias cardinal='sshxy -t s4 ssh -XY cardinal'

# Aliases for GMAO machines
alias polar='sshxy polar'
alias thunder='sshxy thunder'

# Aliases for mounting/unmounting SSHFS volumes
alias mount_www='sshfs polar:/www/html/intranet/personnel/rmahajan /Users/rmahajan/Volumes/polar/www'
alias umount_www='umount /Users/rmahajan/Volumes/polar/www'
alias mount_s4data='sshfs s4:/data/users/rmahajan /Users/rmahajan/Volumes/s4/rmahajan'
alias umount_s4data='umount /Users/rmahajan/Volumes/s4/rmahajan'
#alias mount_nobackup='sshfs discover:/discover/nobackup/rmahajan /discover/nobackup/rmahajan'
#alias umount_nobackup='umount /discover/nobackup/rmahajan'

# Alias for NCEP/NOAA machines
alias theiaTunnel='sshxy -q -L 45545:localhost:45545 theia'
alias theiabTunnel='sshxy -q -L 45545:localhost:45545 theiaBoulder'
alias theiaLocal='sshxy -q theiaLocal'
alias theiaVIAemc='sshxy -q -A -t emc         ssh -q -A theiaLocal'
alias theiaVIAdorne='ssh   -q -A -t dorne       ssh -q -A theiaLocal'
alias theiaVIAdornelocal='ssh   -q -A -t dorne.local ssh -q -A theiaLocal'
alias theiaVIAdornelocalVIAemc='ssh -q -A -t dorne.local ssh -q -A -t emc ssh -q -A theiaLocal'
alias theiaVIAfrostfangslocal='ssh -q -A -t frostfangs.local ssh -q -A theiaLocal'
alias theiaVIAfrostfangslocalVIAemc='ssh -q -A -t frostfangs.local ssh -q -A -t emc ssh -q -A theiaLocal'
function scpToTheia() {   scp $1 theiaLocal:$2; }
function scpFromTheia() { scp theiaLocal:$1 $2; }
function rsyncToTheia() {   rsync -auvx --progress $1 theiaLocal:$2; }
function rsyncFromTheia() { rsync -auvx --progress theiaLocal:$1 $2; }

# Function to toggle Git-LFS and give Status
function toggleGITLFS() {
    if [ -z "$GIT_LFS_SKIP_SMUDGE" ]; then
        echo "Disable Git-LFS"
        export GIT_LFS_SKIP_SMUDGE=1
        return
    fi
    if [ ${GIT_LFS_SKIP_SMUDGE} -eq 0 ]; then
        echo "Disable Git-LFS"
        export GIT_LFS_SKIP_SMUDGE=1
        return
    elif [ ${GIT_LFS_SKIP_SMUDGE} -eq 1 ]; then
        echo "Enable Git-LFS"
        export GIT_LFS_SKIP_SMUDGE=0
        return
    fi
}

function GitLFS-status() {
    if [ -z "$GIT_LFS_SKIP_SMUDGE" ]; then
        echo "Git-LFS is ON"
        return
    fi
    if [ ${GIT_LFS_SKIP_SMUDGE} -eq 0 ]; then
        echo "Git-LFS is ON"
        return
    elif [ ${GIT_LFS_SKIP_SMUDGE} -eq 1 ]; then
        echo "Git-LFS is OFF"
        return
    fi
}

# Aliases in svn-repo and svn-work directories
alias svnrep='cd ${home}/svn-repo; echopwd'
alias svnwork='cd ${home}/svn-work; echopwd'
alias scripts='cd ${home}/svn-work/scripts/trunk/src; echopwd'
alias writing='cd ${home}/svn-work/writing; echopwd'

# Aliases for Git
alias gti='git'
alias gci='git commit'
alias glg='git log --name-only'

# Aliases for SVN
alias sci='svn ci'
alias sdiff='svn diff'
alias sstatus='svn status'
alias slog='svn log'
alias sadd='svn add'
alias spropset='svn propset svn:keywords "Author Id Revision Date"'
alias sup='svn up'
alias sinfo='svn info'

# Aliases and mistypes for make
alias mkae='make'
alias amke='make'
alias akme='make'
alias maek='make'

# Aliases for more and mistypes
alias more='less'
alias mroe='more'
alias lesss='less'

# Aliases for cd and mistypes
alias dc='cd'
alias xs='cd'
alias ..='cd ..'
alias ...='cd ../..'
alias owd='pwd'

# Aliases for vi, vim, gvim and their mis-typos
# MacVim is installed using HomeBrew on a Mac
alias vi='mvim -v'
alias gvim='mvim -g'
alias vim='vi'
alias ci='vi'
alias ci='vi'
alias cim='vi'
alias ciM='vi'
alias givm='gvim'
alias gcim='gvim'
alias givM='gvim'
alias gciM='gvim'
alias gviM='gvim'

# Aliases for tkcvs, tksvn and tkdiff from TwoBarleyCorns
alias tkcvs='/Applications/TkCVS.app/Contents/MacOS/tkcvs.kit'
alias tksvn='/Applications/TkCVS.app/Contents/MacOS/tkcvs.kit'
alias tkdiff='/Applications/TkDiff.app/Contents/MacOS/tkdiff'

# Alias for xxdiff
alias xxdiff='/Applications/xxdiff.app/Contents/MacOS/xxdiff'

# Alias for diff (Use colordiff)
alias diff='colordiff'

# Alias for ps
alias psr='ps -U rmahajan'
alias psax='ps ax | grep -v root'
alias psssh='ps ax | grep ssh'

# Aliases for closing a terminal window
alias lo='exit'

# Aliases for tar
alias ltar='tar -tvf'
alias ctar='tar -cvf'
alias cztar='tar -czvf'
alias xtar='tar -xvf'
alias xztar='tar -xzvf'

# Aliases for open
alias oepn='open'
alias opne='open'

# Aliases for ldd
alias ldd='otool -L'

# Aliases for tail
alias tf='tail -f'

# Aliases for module mis-typos
alias moduel='module'

###############################################################
